import { BrowserRouter } from "react-router-dom";
import Wrapper from "../layout/Wrapper";

const App = () => {
  return (
    <BrowserRouter>
      <Wrapper />
    </BrowserRouter>
  );
};

export default App;
